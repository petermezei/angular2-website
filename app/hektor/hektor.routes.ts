import { RouterConfig } from '@angular/router';

import { HektorComponent } from './hektor.component';

export const HektorRoutes: RouterConfig = [
  { path: 'hektor', component: HektorComponent }
];
